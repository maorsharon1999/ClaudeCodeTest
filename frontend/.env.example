# Copy to .env and set for device/staging builds.
# Expo inlines EXPO_PUBLIC_* variables at bundle time (SDK 49+).

# Physical device on same Wi-Fi (replace with your machine's LAN IP):
EXPO_PUBLIC_API_URL=http://192.168.1.x:3000/api/v1

# Android emulator: emulator cannot reach 192.168.x.x; use 10.0.2.2 instead.
# Use: npm run run:android:emulator  (sets this automatically via cross-env)
# EXPO_PUBLIC_API_URL=http://10.0.2.2:3000/api/v1
